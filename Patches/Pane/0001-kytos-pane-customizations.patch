diff --git a/Sources/pane/PaneProtocol.swift b/Sources/pane/PaneProtocol.swift
index a283f6d..cbded7f 100644
--- a/Sources/pane/PaneProtocol.swift
+++ b/Sources/pane/PaneProtocol.swift
@@ -98,6 +98,7 @@ struct PaneTerminalSnapshot: Codable {
     var cursorY: Int
     var isAlternate: Bool
     var lines: [[PaneCell]]
+    var scrollbackLines: [[PaneCell]]
 }
 
 struct PaneTerminalDelta: Codable {
@@ -105,6 +106,7 @@ struct PaneTerminalDelta: Codable {
     var endY: Int
     var cursorX: Int
     var cursorY: Int
+    var scrolledLines: Int
     var lines: [[PaneCell]]
 }
 
@@ -336,6 +338,13 @@ extension PaneTerminalSnapshot: PaneBinaryCodable {
                 cell.write(to: &writer)
             }
         }
+        writer.writeUInt16(UInt16(scrollbackLines.count))
+        for line in scrollbackLines {
+            writer.writeUInt16(UInt16(line.count))
+            for cell in line {
+                cell.write(to: &writer)
+            }
+        }
     }
 
     init(from reader: inout PaneBinaryReader) throws {
@@ -356,6 +365,22 @@ extension PaneTerminalSnapshot: PaneBinaryCodable {
             }
             lines.append(line)
         }
+        if reader.remaining >= 2 {
+            let scrollbackCount = Int(try reader.readUInt16())
+            scrollbackLines = []
+            scrollbackLines.reserveCapacity(scrollbackCount)
+            for _ in 0..<scrollbackCount {
+                let cellCount = Int(try reader.readUInt16())
+                var line: [PaneCell] = []
+                line.reserveCapacity(cellCount)
+                for _ in 0..<cellCount {
+                    line.append(try PaneCell(from: &reader))
+                }
+                scrollbackLines.append(line)
+            }
+        } else {
+            scrollbackLines = []
+        }
     }
 }
 
@@ -365,6 +390,7 @@ extension PaneTerminalDelta: PaneBinaryCodable {
         writer.writeUInt16(UInt16(endY))
         writer.writeUInt16(UInt16(cursorX))
         writer.writeUInt16(UInt16(cursorY))
+        writer.writeUInt16(UInt16(scrolledLines))
         writer.writeUInt16(UInt16(lines.count))
         for line in lines {
             writer.writeUInt16(UInt16(line.count))
@@ -379,6 +405,7 @@ extension PaneTerminalDelta: PaneBinaryCodable {
         endY = Int(try reader.readUInt16())
         cursorX = Int(try reader.readUInt16())
         cursorY = Int(try reader.readUInt16())
+        scrolledLines = Int(try reader.readUInt16())
         let lineCount = Int(try reader.readUInt16())
         lines = []
         lines.reserveCapacity(lineCount)
diff --git a/Sources/pane/PaneTerminalSession.swift b/Sources/pane/PaneTerminalSession.swift
index 03ae275..e191bd5 100644
--- a/Sources/pane/PaneTerminalSession.swift
+++ b/Sources/pane/PaneTerminalSession.swift
@@ -22,6 +22,7 @@ final class PaneTerminalSession: Terminal, LocalProcessDelegate {
     private var lastExitCode: Int32?
     private let logger: Logger
     private var subscribers: [UUID: PaneServerClient] = [:]
+    private var lastYBase: Int = 0
 
     init(id: String, name: String?, options: TerminalOptions = TerminalOptions.default) {
         self.id = id
@@ -45,13 +46,14 @@ final class PaneTerminalSession: Terminal, LocalProcessDelegate {
         }
 
         var environment = ProcessInfo.processInfo.environment
-        environment["TERM"] = environment["TERM"] ?? "xterm-256color"
-        environment["COLORTERM"] = environment["COLORTERM"] ?? "truecolor"
+        environment["TERM"] = "xterm-256color"
+        environment["COLORTERM"] = "truecolor"
         environment["LANG"] = environment["LANG"] ?? "en_US.UTF-8"
 
         let envArray = environment.map { "\($0.key)=\($0.value)" }
+        let home = environment["HOME"] ?? NSHomeDirectory()
         logger.info("starting process", metadata: ["executable": "\(shell)", "args": "\(args)"])
-        process.startProcess(executable: shell, args: args, environment: envArray)
+        process.startProcess(executable: shell, args: args, environment: envArray, currentDirectory: home)
     }
 
     func terminate() {
@@ -153,11 +155,14 @@ final class PaneTerminalSession: Terminal, LocalProcessDelegate {
 
     func sendSnapshot(to client: PaneServerClient) {
         let snapshot = makeSnapshot()
+        lastYBase = getScrollbackInfo().count
         client.send(.snapshot(snapshot))
     }
 
     private func sendDeltaIfNeeded() {
         guard !subscribers.isEmpty else {
+            let currentYBase = getScrollbackInfo().count
+            lastYBase = currentYBase
             clearUpdateRange()
             return
         }
@@ -170,7 +175,10 @@ final class PaneTerminalSession: Terminal, LocalProcessDelegate {
             clearUpdateRange()
             return
         }
-        let delta = makeDelta(startY: safeStart, endY: safeEnd)
+        let currentYBase = getScrollbackInfo().count
+        let scrolled = max(0, currentYBase - lastYBase)
+        lastYBase = currentYBase
+        let delta = makeDelta(startY: safeStart, endY: safeEnd, scrolledLines: scrolled)
         clearUpdateRange()
         for client in subscribers.values {
             client.send(.delta(delta))
@@ -182,17 +190,38 @@ final class PaneTerminalSession: Terminal, LocalProcessDelegate {
         let lines = (0..<rows).map { row in
             buildLine(row: row)
         }
+        let (sbStart, sbCount) = getScrollbackInfo()
+        let scrollbackCount = min(sbCount, 500)
+        logger.info("[Scrollback] getScrollbackInfo → start=\(sbStart) count=\(sbCount) scrollbackCount=\(scrollbackCount)")
+        var scrollbackLines: [[PaneCell]] = []
+        if scrollbackCount > 0 {
+            scrollbackLines.reserveCapacity(scrollbackCount)
+            let startIdx = sbStart + (sbCount - scrollbackCount)
+            var nilCount = 0
+            for i in 0..<scrollbackCount {
+                if let bufferLine = getScrollInvariantLine(row: startIdx + i) {
+                    scrollbackLines.append(buildLineFromBuffer(bufferLine))
+                } else {
+                    nilCount += 1
+                }
+            }
+            if nilCount > 0 {
+                logger.warning("[Scrollback] \(nilCount)/\(scrollbackCount) lines returned nil from getScrollInvariantLine (startIdx=\(startIdx))")
+            }
+        }
+        logger.info("[Scrollback] snapshot: \(scrollbackLines.count) scrollback lines collected")
         return PaneTerminalSnapshot(
             cols: cols,
             rows: rows,
             cursorX: cursor.x,
             cursorY: cursor.y,
             isAlternate: isCurrentBufferAlternate,
-            lines: lines
+            lines: lines,
+            scrollbackLines: scrollbackLines
         )
     }
 
-    private func makeDelta(startY: Int, endY: Int) -> PaneTerminalDelta {
+    private func makeDelta(startY: Int, endY: Int, scrolledLines: Int) -> PaneTerminalDelta {
         let cursor = getCursorLocation()
         let lines = (startY...endY).map { row in
             buildLine(row: row)
@@ -202,6 +231,7 @@ final class PaneTerminalSession: Terminal, LocalProcessDelegate {
             endY: endY,
             cursorX: cursor.x,
             cursorY: cursor.y,
+            scrolledLines: scrolledLines,
             lines: lines
         )
     }
@@ -227,4 +257,20 @@ final class PaneTerminalSession: Terminal, LocalProcessDelegate {
         }
         return cells
     }
+
+    private func buildLineFromBuffer(_ bufferLine: BufferLine) -> [PaneCell] {
+        let lineWidth = bufferLine.count
+        var cells: [PaneCell] = []
+        cells.reserveCapacity(lineWidth)
+        for col in 0..<lineWidth {
+            let data = bufferLine[col]
+            let character = String(getCharacter(for: data))
+            cells.append(PaneCell(
+                char: character,
+                width: data.width,
+                attribute: PaneAttribute(data.attribute)
+            ))
+        }
+        return cells
+    }
 }
diff --git a/Sources/pane/main.swift b/Sources/pane/main.swift
index 820482f..e965011 100644
--- a/Sources/pane/main.swift
+++ b/Sources/pane/main.swift
@@ -8,6 +8,9 @@ struct GlobalOptions: ParsableArguments {
 
     @Flag(name: .customLong("no-auto-start"), help: "Don't auto-start the server for client commands.")
     var noAutoStart = false
+
+    @Option(name: .customLong("socket"), help: "Override the Unix socket path used by the server/client.")
+    var socket: String?
 }
 
 struct PaneContext {
@@ -22,7 +25,7 @@ struct PaneContext {
             logger.info("logging enabled", metadata: ["path": "\(logPath)"])
         }
         self.logger = logger
-        self.socketPath = try PanePaths.socketPath()
+        self.socketPath = try options.socket ?? PanePaths.socketPath()
         self.logToFile = options.log
     }
 
